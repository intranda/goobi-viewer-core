/**
 * Provides URL resolution and navigation history support for the Goobi viewer web application.
 * {@code ViewerPath} models a parsed request URL, {@code ViewerPathBuilder} constructs paths from HTTP requests,
 * and {@code ViewHistory} tracks recently visited views to enable back-navigation.
 */
package io.goobi.viewer.model.urlresolution;
