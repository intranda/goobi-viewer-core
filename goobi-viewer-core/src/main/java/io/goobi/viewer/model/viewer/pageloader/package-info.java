/**
 * Provides strategies for loading record pages, with eager, lean, and empty implementations of the {@code IPageLoader} interface.
 * Page loaders decouple the {@code ViewManager} from the specific mechanism used to fetch {@code PhysicalElement} data from the Solr index.
 */
package io.goobi.viewer.model.viewer.pageloader;
