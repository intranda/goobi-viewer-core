/**
 * Servlet and JAX-RS filters for the Goobi viewer. Handles cross-cutting concerns such as
 * session initialisation, access control, CORS headers, and request logging applied
 * transparently to incoming HTTP requests.
 */
package io.goobi.viewer.filters;
