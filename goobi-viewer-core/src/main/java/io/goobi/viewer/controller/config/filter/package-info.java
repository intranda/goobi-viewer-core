/**
 * Configuration-driven value filters used to include or exclude data based on rules defined in the viewer configuration.
 * Provides an interface and several implementations for filtering string values, supporting both pass-through and
 * configured matching strategies.
 */
package io.goobi.viewer.controller.config.filter;
