<!DOCTYPE suppressions PUBLIC
     "-//Checkstyle//DTD SuppressionFilter Configuration 1.0//EN"
     "https://checkstyle.org/dtds/suppressions_1_0.dtd">
 
<suppressions>
	<!-- The Apache lib used is throwing the general Exception... -->
	<suppress checks="IllegalCatch"
		  files="MessageQueueManager.java"
		  lines="250-650"/>
	<!-- Best-effort filter: any exception in the prewarm path must never break tile delivery,
	     so a wide RuntimeException guard around the submit call is intentional. -->
	<suppress checks="IllegalCatch"
		  files="PrewarmRequestFilter.java"/>
</suppressions>
