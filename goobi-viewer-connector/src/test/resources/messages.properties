# Stand-in for the viewer's message bundle. The record generation translates metadata labels through
# ViewerResourceBundle; in an installation the bundle comes from the viewer, in tests an empty one is enough
# because the conformance tests assert the protocol envelope, not translated labels.
